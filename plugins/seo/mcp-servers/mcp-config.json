{
  "_comment": "SEO Plugin MCP Servers - Use 'npx claudeup' for easy setup",

  "google-analytics": {
    "command": "npx",
    "args": ["-y", "mcp-server-google-analytics"],
    "env": {
      "GOOGLE_APPLICATION_CREDENTIALS": "${GOOGLE_APPLICATION_CREDENTIALS}",
      "GA_PROPERTY_ID": "${GA_PROPERTY_ID}"
    },
    "description": "Google Analytics 4 data extraction for page performance metrics"
  },

  "google-search-console": {
    "command": "npx",
    "args": ["-y", "mcp-server-gsc"],
    "env": {
      "GOOGLE_APPLICATION_CREDENTIALS": "${GOOGLE_APPLICATION_CREDENTIALS}",
      "GSC_SITE_URL": "${GSC_SITE_URL}"
    },
    "description": "Google Search Console for search performance and Core Web Vitals"
  },

  "seo-data-api-mcp": {
    "command": "docker",
    "args": ["run", "-i", "--rm", "-e", "SERANKING_API_TOKEN=${SERANKING_API_TOKEN}", "seranking/seo-data-api-mcp-server:latest"],
    "env": {
      "SERANKING_API_TOKEN": "${SERANKING_API_TOKEN}"
    },
    "description": "Official SE Ranking MCP server for keyword rankings, backlinks, and competitor analysis",
    "_requires": "Docker must be installed and running"
  }
}
